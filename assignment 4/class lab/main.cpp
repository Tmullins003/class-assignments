#include <iostream>

using namespace std;

int main()
{

/* semi pseudo code
    int negtotal = 0
    int postotal = 0
    int negcounter = 0
    int poscounter = 0
    prompt for input

    while (input != 0)
        if (input < 0)
            negtotal = negtotal + input
            negcounter = negcounter + 1
        if (input > 0)
            postotal = postotal + input
            poscounter = postotal +1
        prompt for and enter input
    negavg = negtotal/negcounter
    posavg = postotal/poscounter

    cout << sum of negnumbers << sum of posnumbers << avg of negnumbers << avg of posnumbers

*/
int negtotal = 0
int postotal = 0









    return 0;
}
